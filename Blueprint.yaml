ProjectName: blueprint-project-example
Description: Some descr

Template:
  path: "./"

Modules:
  logger:
    name: zap
  env:
    vars:
      - name: APP_NAME
        type: string
        default: "#"
